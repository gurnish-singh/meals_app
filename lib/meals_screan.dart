import 'package:flutter/material.dart';

class MealsScrean extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('recipies'),
      ),
      body: Center(
        child: Text('recipy for the category'),
      ),
    );
  }
}
